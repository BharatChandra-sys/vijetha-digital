name: Deploy

on:
  push:
    branches: ["main"]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build docker image
        run: docker build -t vijetha-digital-backend:latest .

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Deployment placeholder
        run: echo "Configure registry + SSH deployment secrets and commands here."
